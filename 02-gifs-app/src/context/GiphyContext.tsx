import { createContext } from "react";
// import { giphyApi } from "../services/giphyApi";

export const GiphyContext = createContext(null);

// const { response } = giphyApi;

// export const GiphyProvider = ({ children }) => {
//   return;
// };
